

def process_command(command, tgid):
    """
    Identify the command into the available commands and execute it.

    Args:
        command (str): The command to execute
        tgid (int): Telegram id of the user

    Returns:
        str: Result of executing the command
    """
    return command